# 01 · 当前进度与下一步计划

> 2026-07-09 更新（v3）。Transfermarkt 数据已集成：身高 + 身价 + 详细位置（92% 匹配率）。增强字段仅剩 weight 缺失。

## 一、已完成：阶段 0-2（数据管线）

### 阶段 0 · 数据采集

| 设计文档 | 实际落地 | 偏差说明 |
|----------|----------|----------|
| ScraperFC 抓取 FBref + Transfermarkt | Kaggle 数据集 `emrey3lmaz/top-5-league-football-player-stats-2017-2025`（2024-25 赛季切片） | ~~ScraperFC v4.5.0 导入报错~~（已确认为瞬时网络问题，导入可成功）；但 v4.x 用真实浏览器绕过 Cloudflare，当前 Windows 环境浏览器连接失败（WinError 10061）。换老 Python 版本无效——这是浏览器自动化环境问题，非 Python 兼容性问题 |
| 10 联赛 × 3 赛季 | 5 大联赛 × 1 赛季（2024-25） | 数据集含 8 赛季历史数据，后续可扩展 |
| Transfermarkt 主位置匹配 | 已实现 | 姓名匹配（TM 俱乐部名为德语全名，与 FBref 英文短名几乎无交集，改用纯姓名匹配），匹配率 92% |

**实际产出**：`data/raw/players.csv`（2854 球员，FBref 178 列 + TM 身高/身价/详细位置）

**保留资产**：
- `src/collector.py` 中 `flatten_columns`、`normalize_league_name`、`merge_stat_categories`、`attach_position`、`norm_name`、`attach_transfermarkt_data` 等纯函数仍可用于后续数据加工
- `config/league_mapping.yaml` 已覆盖 Kaggle 数据集的联赛名格式

### 阶段 1 · 数据读取与字段检查

完全按设计文档实现，无偏差。

| 项 | 状态 |
|----|------|
| `src/data_loader.py` — 字段映射、三级字段检查、CSV 加载 | 完成 |
| `config/field_mapping.yaml` — 含 Kaggle 数据集列名别名 | 完成 |
| 14 个测试 | 全部通过 |

**字段检查结果**（当前数据集）：

| 等级 | 齐全 | 缺失 |
|------|------|------|
| 必须字段 | 全部 6 项 | — |
| 核心评分字段 | **全部 10 项** | — |
| 增强字段 | xg, xa, progressive_carries, shot_creating_actions, height, market_value | **weight**（Transfermarkt 无此数据） |

### 阶段 2 · 数据清洗与位置映射

完全按设计文档实现，增加了一个预处理步骤。

| 模块 | 状态 |
|------|------|
| `src/cleaner.py` — 年龄计算/筛选、分钟数标记、缺失处理、去重、`clean()` 管线 | 完成 |
| `src/position_mapper.py` — TM 位置映射 + 启发式兜底 | 完成 |
| `config/position_mapping.yaml` | 完成 |
| 28 个测试 | 全部通过 |

**新增**：`_ensure_per90()` 在位置映射前从原始计数字段 + minutes 计算 `key_passes_per90` / `def_actions_per90`，使启发式能正确区分 AM/CM。

**清洗后数据统计**：

| 指标 | 数值 |
|------|------|
| 总 U21 球员（评分位置） | 508 |
| 正式评分（≥600 分钟） | 186 |
| 观察名单（<600 分钟） | 322 |
| 位置分布 | Winger 249 / CM 105 / AM 78 / DM 76 |
| 联赛分布 | Ligue 1 140 / La Liga 115 / Premier League 95 / Serie A 90 / Bundesliga 68 |
| 位置来源 | transfermarkt 63.8% / heuristic 36.2% |
| 身高覆盖 | 87.8% |
| 身价覆盖 | 82.7% |

**位置分布说明**：TM 集成后 AM 从 9 升至 78（Ethan Nwaneri、Bellingham、Musiala 等正确归入 AM），CM/DM 更均衡，位置准确性大幅提升。

### 测试总览

```
61 passed, 0 warnings
├── tests/test_collector.py        19 passed
├── tests/test_data_loader.py      14 passed
├── tests/test_cleaner.py          14 passed
└── tests/test_position_mapper.py  14 passed
```

### 项目文件结构

```
Project4Nathon/
├── main.py                          # 管线入口（--skip-collect 跳过采集）
├── config/
│   ├── config.yaml                  # 全局运行参数
│   ├── field_mapping.yaml           # 字段别名映射（含 Kaggle 列名）
│   ├── league_mapping.yaml          # 联赛名归一化（含 Kaggle 格式）
│   └── position_mapping.yaml        # 位置分组映射
├── src/
│   ├── config_loader.py             # YAML 配置加载
│   ├── collector.py                 # 采集纯函数（平坦化/合并/匹配）
│   ├── data_loader.py               # 数据读取 + 字段检查
│   ├── cleaner.py                   # 清洗管线
│   └── position_mapper.py           # 位置映射
├── tests/
│   ├── conftest.py                  # 共享夹具（合成 FBref/TM 数据）
│   ├── test_collector.py
│   ├── test_data_loader.py
│   ├── test_cleaner.py
│   └── test_position_mapper.py
├── data/
│   ├── raw/players.csv              # 原始数据（2966 行）
│   └── processed/cleaned_players.csv # 清洗后数据（514 行）
└── docs/                            # 设计文档
```

---

## 二、待改进（阶段 0-2 遗留问题）

### 2.1 只覆盖 1 个赛季（优先级：低）

设计文档要求 3 个赛季（2023-24 ~ 2025-26），当前只有 2024-25。FBref 数据集含 8 赛季历史数据，扩展即可。

---

## 三、下一步：阶段 3-9

### 阶段 3 · 特征计算（下一步）

按 `docs/阶段3-特征计算.md` 实现 `feature_calculator.py`：

- 计算 per90 指标：goals_per90, assists_per90, dribbles_per90, tackles_per90 等
- 计算复合指标：进攻参与度、防守贡献度、效率值
- 归一化到 0-100 分制
- 输出 `data/processed/features.csv`

### 阶段 4 · 潜力评分模型

按 `docs/阶段4-潜力评分模型.md` 实现 `scorer.py`：

- Winger / AM / CM / DM 分位置权重
- 加权总分公式
- 输出 `data/processed/scores.csv`

### 阶段 5 · Opta 联赛强度修正

按 `docs/阶段5-Opta联赛强度修正.md` 实现 `league_correction.py`。

### 阶段 6 · 候选清单与排行榜

按 `docs/阶段6-候选清单与排行榜.md` 实现 `ranker.py`：
- Top 20 正式榜
- 观察名单
- 分位置榜

### 阶段 7-9 · 雷达图 + 报告生成 + 答辩材料

按对应文档推进。

---

## 四、即刻可做

```bash
# 运行完整管线
python main.py                  # 首次运行（自动下载 Kaggle 数据集）
python main.py --skip-collect   # 已有数据时跳过采集

# 运行测试
python -m pytest tests/ -v

# 查看清洗结果
python -c "import pandas as pd; df=pd.read_csv('data/processed/cleaned_players.csv'); print(df.describe())"
```
